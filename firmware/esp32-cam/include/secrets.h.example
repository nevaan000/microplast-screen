#pragma once

#define WIFI_SSID "your-wifi-name"
#define WIFI_PASSWORD "your-wifi-password"
#define DEVICE_NAME "microplast-cam"

// Optional: enables the /send endpoint for direct uploads to MicroPlast Screen.
#define SERVER_URL "http://192.168.1.100:8000"
#define DEVICE_API_KEY "replace-with-the-device-api-key"

// Optional external LED transistor-driver pin. GPIO13 is generally safer than GPIO12.
// #define EXTERNAL_LED_PIN 13
